# 운영 환경(prod)용 설정
server:
  address: 0.0.0.0 # Render 환경에서 외부 요청을 받을 수 있도록 설정

spring:
  jpa:
    properties:
      hibernate:
        format_sql: false
        show_sql: false

logging:
  level:
    root: warn
    com.lineupmaker: info
    org.springframework.security: warn
