# 운영 환경용 설정 (로그 최소화)
spring:
  jpa:
    properties:
      hibernate:
        format_sql: false
        show_sql: false

logging:
  level:
    root: warn
    com.lineupmaker: info # 우리 애플리케이션 패키지는 info 레벨 유지
    org.springframework.security: warn
